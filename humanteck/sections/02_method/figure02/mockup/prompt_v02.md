# Layout v02 — integrated magnifying inset

Built-in imagegen edit. Input: `layout_v01.png`. Candidate selection remains open.

+Use case: scientific-educational
Asset type: revised academic qualitative-comparison LAYOUT MOCKUP, not experimental evidence.
Input image 1 is the previous layout to revise. Retain the same generic laboratory drawer-and-monitor scene, the three method columns, the exact same image quality in all three columns, serif Times New Roman typography, white page background.
Primary change: make a compact wide strip with target canvas 2400 x 850 (approximately 2.8:1). Remove the separate Full view and Detail rows and labels. Each of three equal side-by-side method panels contains ONE main scene image with ONE integrated magnifying inset that slightly extends below the main image. The entire figure should be much wider and shorter than input image 1.
Headers verbatim: "VIGS-SLAM", "Ours", "Ground truth".
Per-panel layout: main scene image preserves natural aspect ratio and camera pose, no stretching. Mark a small rectangular source ROI around ONLY two drawer labels on the left side of the scene with a fine amber dashed rectangle. Show exactly that same crop enlarged in an amber outlined rectangular inset toward the lower-right part of its own panel, slightly overlapping the main image's bottom edge. Use two thin amber dashed corner-to-corner leader lines from the small ROI's right-side corners to the large inset's left-side corners, forming an unfilled trapezoid-like magnification connector. They must not cross each other, not go through the inset, and must never connect different method panels. The crop image stays rectangular and front-facing: no perspective warping and no imagery inside the trapezoid. Keep useful scene context visible and source ROI unobscured.
The three columns have identical main images, identical crop content and resolution, identical inset positions and scales. All scene content is fictitious placeholder content; do NOT invent improved performance for Ours or degrade VIGS-SLAM. No PSNR numbers, checkmarks, performance statements or extra method labels.
Inset occupies about 55% of each panel's width, large enough to judge visual hierarchy. Only one source ROI and one inset per panel. Minimize whitespace, no title, no large bottom caption.
Small footer verbatim: "Layout mockup - not experimental results".
High-quality restrained scientific publication layout. Strictly preserve factual distinction between layout and results.
