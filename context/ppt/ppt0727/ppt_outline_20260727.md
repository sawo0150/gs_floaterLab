# PPT 구성안 — VIGS-SLAM 실시간화 여정 (exp52 → exp56, 07/20 → 07/27)

> 대상 청중: 07/20 발표(`context/ppt/ppt0720/`, VIGS-SLAM 채택·타임라인 분석)를 이미 본 사람.
> 그 발표의 결론(VIGS-SLAM 채택, 20초 타이밍 버그 수정, `gs_parallel` 도입, mapping이 81.4%
> 병목이라는 진단, 실시간 배수 1.52배)은 전제로 깔고 **exp52 전체를 간단히 리캡한 뒤, 그 이후
> 일주일(exp53~56)간 무엇을 했는지**를 다룬다.
> 스토리라인: **지난 발표 리캡(exp52) → 실시간 최초 돌파(exp53+54) → 품질까지 함께(exp55) →
> "왜 안 빨라지나" 심층 규명 + 4단계 연속 개선(exp56) → 종합 스코어카드 → 다음 스텝**
> 총 19장. 각 슬라이드에 [시각자료]와 [발표 노트] 포함.
>
> **톤 원칙**: 이번 사이클은 정량적 성과가 뚜렷하다(실시간 최초 돌파 + 시간 대폭 절감 + 품질
> 동반 개선) — 과장할 필요는 없지만 숨길 필요도 없다. 동시에 **실패한 시도(Phase 3, 6, 8b)도
> 숨기지 않고 보여준다** — "왜 안 됐는지"까지 규명한 게 이 사이클의 방법론적 신뢰도를 보여주는
> 지점이라는 게 핵심 메시지.

---

## §0. 도입 (슬라이드 1–3)

### 슬라이드 1 — 표지
- 제목: **"Incremental 3DGS Mapping 실시간화 — exp52~56 (07/20 → 07/27)"**
- 부제: VIGS-SLAM 채택 리캡 · 실시간 최초 돌파 · 품질까지 함께 개선 · "왜 안 빨라지나" 심층 규명
- [시각자료] 표지는 텍스트만

### 슬라이드 2 — 지난 발표(07/20) 리캡: 어디까지 끝냈나
- **VIGS-SLAM(ECCV2026) 채택**: 단안 RGB+IMU, DROID-SLAM식 dense correlation 트래킹 — 우리 목표(흑백 SLAM 트래킹 + RGB incremental 매핑)에 가장 가까운 참조 아키텍처
- **20초 타이밍 버그 발견·수정**: reader 프로세스 `sleep(20)` 순서 문제로 모든 "온라인 루프 총합"에 인위적 20초가 섞여있었음 — 수정 후 실시간 배수 **2.77배 → 1.52배**로 정정
- **구조적 개선**: IMU 프리적분 C++ 빌드 + TensorRT 3종(−14%), tracking/mapping 비동기 오버랩 `gs_parallel` 도입(업스트림 레이스 컨디션 버그 발견·수정, −26.1% 추가)
- **핵심 진단**: mapping(rasterize+backward+loss_compute)이 온라인 루프의 **81.4%**로 압도적 병목 — 이번 사이클(exp53~56) 전체가 이 진단 위에서 진행됨
- [시각자료] 07/20 발표의 `fig_timeline_parallel.png`/`fig_gsmapping_granular.png` 재사용
- [발표 노트] "오늘은 이 지점(1.52배, 아직 실시간 아님) 이후 일주일간 일어난 일만 다룹니다"

### 슬라이드 3 — 이번 사이클 요약 (TL;DR)
- **exp53+54**: 트래킹·매핑 양쪽 경량화 → **실시간 최초 돌파(0.94배)**
- **exp55**: 내용-적응 gaussian 예산 + carve loss 온라인 이식 → 시간 유지하며 **평균 gaussian −35.9%, 가시 floater −7.5%**
- **exp56**: "gaussian을 줄여도 왜 안 빨라지나"를 회귀분석으로 규명 → 숨어있던 진짜 병목 2건 발견·수정 → **0.70배, PSNR까지 +0.88/+0.93dB 개선**
- 한 줄 요약: **"1.52배(비실시간) → 0.70배(여유 19초), 그 사이 품질은 계속 좋아짐"**
- [시각자료] 실시간 배수 추이 막대(1.52 → 0.94 → 0.70, 1.0배 기준선 강조)
- [발표 노트] 이후 슬라이드가 이 요약을 하나씩 풀어서 보여줌

---

## §1. 실시간 최초 돌파 — exp53 + exp54 (슬라이드 4–6)

### 슬라이드 4 — exp53: 프론트엔드(트래킹) 경량화
- 07/20 진단(mapping 81.4%)과 별개로, **트래킹도 손댈 여지가 컸음** — 나중 exp56 부록에서 "순수 트래킹 절감폭(−47.5%)이 매핑 절감폭(−12~15%)보다 훨씬 크다"는 게 재확인됨
- 축A: correlation 반복횟수(`iters1`/`iters2`) 4/2→1/0 — −20.7%
- 축B: `motion_filter.thresh` 2.4→3.6(keyframe 발생률 자체를 낮춤) — **−15.4%, 가장 큰 단일 레버**
- 축C: `frontend_window`/`radius` 25/2→15/1 — −1.7%
- 축D(correlation 해상도)는 사전학습 가중치에 shape가 고정 결합돼 조사 후 **구현 불가 판정** — 재학습 없인 못 건드림, 정직하게 기각 기록
- [시각자료] 축A~D 절감폭 막대(D는 회색으로 "기각" 표시)
- [발표 노트] "재학습 없이 안 되는 것도 시도해보고 명확히 접었다는 게 중요 — 막연히 미룬 게 아니라 판정을 끝냄"

### 슬라이드 5 — exp54: 매핑 경량화
- 축1(`pcd_downsample` 64→128) 채택 — 품질·궤적 손실 없이 −3.3%
- 축2/3/5(init 밀도, iters 하향, `max_viewpoints` 축소)는 **전부 기각** — 이 시점(tracking-bound 규명 근거)에선 효과 없음/역효과
- 축4(`render_downsample`) — 유효하지만(−4.2%, −0.8dB) 이미 실시간이라 비용 감수할 이유 없어 **미채택, 코드만 보존**
- 축7(PPM, content-adaptive 샘플링) 채택 — **동일 예산에서 +0.16dB 공짜**
- [시각자료] 7축 결과표(채택/기각/보류 색상 구분)
- [발표 노트] "속도 축만 스캔한 게 아니라 축3에서 우연히 '이 시점엔 트래킹이 91%'라는 걸 발견 — 이게 exp53 우선순위의 직접 근거가 됨(서로 다른 실험이 서로의 근거를 만든 사례)"

### 슬라이드 6 — 결과: 실시간 최초 돌파
- exp53+exp54 통합 레시피: **61.34초, 실시간 배수 0.94배** — 이 프로젝트 최초로 1.0배 미만 달성
- [시각자료] 1.52배 → 0.94배 막대, 1.0배 기준선 통과 강조
- [발표 노트] "여기서 멈춰도 목표(실시간)는 달성인데, 다음 두 사이클(exp55/56)에서 여유를 더 벌고 품질까지 올림"

---

## §2. 품질까지 함께 — exp55 (슬라이드 7–9)

### 슬라이드 7 — 내용-적응 gaussian 예산
- 가설: 디테일 많은(Sobel gradient 큰) keyframe엔 gaussian이 더 필요하고, 단조로운 keyframe엔 덜 필요할 것
- Phase1(캘리브레이션): dense/sparse 두 전역 예산으로 실측 → "밀도로 얻는 이득"과 Sobel 평균의 **Pearson r=0.538** — 가설 확인
- Phase2(적응 예산 구현): 배율곡선 + keyframe별 명시적 cap 신규 구현 → **평균 gaussian 수 −35.9%, 최종 −35.3%, PSNR·궤적 손실 없음(오히려 소폭 개선)**
- [시각자료] Sobel 평균 vs 밀도 이득 산점도(r=0.538 표기), 적응 전/후 평균 gaussian 수 막대
- [발표 노트] "사용자가 낸 아이디어를 먼저 상관관계로 검증한 뒤 구현 — 감으로 가지 않았다는 걸 보여주는 슬라이드"

### 슬라이드 8 — carve loss 온라인 이식
- 배치 트랙에서 이미 검증된(AUC 0.98) carve loss를, VIGS의 BA-정제 추적 depth(`disps_up`)를 신뢰 표면 삼아 **depth-violation 전용 온라인 근사**로 재설계(voxel field 방식은 배치 전용이라 그대로 못 씀)
- region GT가 VIGS 좌표계에 안 맞아 — carve_loss.py 자신의 신호 설계를 그대로 재구현한 **오프라인 진단 지표를 새로 제작**해 검증
- 결과: **가시 floater 수/비율/평균 score 네 지표 전부 일관 개선(−4~8%), PSNR·시간 비용 없음**
- [시각자료] carve on/off 가시 floater 비율 막대(18.76%→17.35%)
- [발표 노트] "표준 지표가 안 맞으면 새로 만들어서라도 채택/기각을 숫자로 판단한다는 원칙"

### 슬라이드 9 — 부록: 직렬 실행으로 밝혀진 진짜 구조
- "지금 상태로 직렬 돌리면 순수 시간이 어떻게 되나" 실측 → **순수 tracking 27.9초 vs 순수 mapping 80.1초**
- 이게 exp54의 "tracking-bound" 결론과 정면 모순 — 파고든 결과 **두 가지 발견**: ①GPU 경합이 병렬 tracking을 거의 2배로 부풀림 ②`_gs_queue`가 가득 차면 오래된 keyframe을 버리는 정책 때문에 **병렬 모드가 mapping 업데이트의 약 80%를 그냥 스킵**하고 있었음(직렬 110회 vs 병렬 22회)
- [시각자료] 직렬 vs 병렬 시간 분해 스택바, map() 호출 횟수 110→22 화살표
- [발표 노트] "이 발견이 exp56 전체의 출발점 — '병렬 배수만 보면 안 되고 안에서 뭐가 스킵되는지 봐야 한다'는 교훈이 다음 사이클로 이어짐"

---

## §3. "왜 gaussian을 줄여도 안 빨라지나" — exp56 (슬라이드 10–16)

### 슬라이드 10 — 문제 제기
- exp55에서 평균 gaussian 수를 35.9% 줄였는데 순수 mapping 시간은 **12.2%밖에 안 줄었음** — "gaussian 수를 줄이는" 접근이 한계에 도달한 신호
- exp54 축6+2에서도 이미 같은 현상 관측(gaussian 수를 더 눌러도 시간 그대로)
- **이번 사이클의 질문: gaussian 수가 시간을 안 줄이면, 무엇이 시간을 지배하는가?**
- [시각자료] gaussian 수 절감폭(−35.9%) vs 시간 절감폭(−12.2%) 막대 대비
- [발표 노트] "숫자가 안 맞아떨어지는 걸 그냥 넘기지 않고 파고든 게 exp56 전체의 시작"

### 슬라이드 11 — Phase 0: 기존 계측 재분석으로 원인 진단
- 새 실험 없이 **기존 타이밍 로그를 처음으로 세부 태그별 집계** — 직렬 순수 map() 68.16초 중 rasterize 40% + backward 34% + loss_compute 24%
- loss_compute는 순수 픽셀 연산(N-무관), rasterize/backward도 이 gaussian 규모(85k~130k)에선 **"고정비"가 데이터量-비례 항을 압도**함을 확인
- [시각자료] map() 시간 구성 파이차트(rasterize/backward/loss_compute/기타)
- [발표 노트] "새 실험을 돌리기 전에 이미 있는 데이터부터 다시 봤다 — 이게 다음 발견들의 방법론"

### 슬라이드 12 — Phase 1: `iters` 하향 — 첫 큰 개선
- "고정비가 지배적"이라면 진짜 변수는 gaussian 수가 아니라 **"반복 횟수(iters)"** — keyframe당 SGD 반복 10→7→5 스캔
- **iters=7: 시간 −16.1%, PSNR mean/kf 둘 다 +0.21dB, map() 처리 keyframe 수도 22→26회 증가 — 전 지표 동시 개선**
- (참고) 반대 방향(iters를 올려보는 실험)은 오히려 coverage가 줄어 역효과 — 방향을 양쪽 다 확인
- [시각자료] iters=10/7/5 시간·PSNR 동시 그래프(7이 sweet spot)
- [발표 노트] "예산이 남으면 반복을 늘리는 게 당연해 보이는데, 큐 드롭 정책 때문에 정반대가 맞았다"

### 슬라이드 13 — Phase 4: 숨어있던 진짜 병목 발견
- `map()` 호출을 세부 로그로 종류별 분해해보니 — **26회 중 단 2~3회(맵 최초 초기화 + IMU 재초기화)가 전체 mapping 시간의 49%를 차지**하고 있었음
- 원인: IMU 재초기화 시 맵 전체를 삭제하고 처음부터 다시 채우는데, 이때 반복 횟수가 90~131회(정규 호출의 10배 이상)
- `init_itr_num` 1050→600으로 낮춰 **추가 시간 −6.2%, PSNR 사실상 무손실**(300까지 내리면 실손실 확인 후 기각 — 600이 스위트스팟)
- [시각자료] map() 호출 26회 중 "초기화급" 2~3회의 시간 비중(49%) 강조 도넛차트
- [발표 노트] "호출 26개 중 딱 2~3개에 절반이 몰려있었다는 게 이번 사이클 최대 발견"

### 슬라이드 14 — Phase 5~7: 회귀분석으로 규명한 진짜 관계식, 그리고 품질까지
- 세션 전체 548개 실제 `map()` 호출 데이터로 회귀분석 — **`iters×n_view`(반복 횟수×카메라 수)가 시간을 지배, gaussian 수·해상도는 부차적**임을 계수로 확정(R²=0.93~0.998)
- "그럼 카메라 뷰를 늘리면 품질이 좋아지지 않을까?" → **window(프론티어) 자체를 키우면 오히려 PSNR 악화**(−1.1~3.5dB, 프론티어가 과거 keyframe과 gradient 예산을 나눠 쓰게 됨) — **기각**
- 대신 **프론티어는 그대로 두고 과거-keyframe 곁눈질 비중만 늘리기** → PSNR mean/kf 둘 다 개선, 시간 비용 무시할 수준 — **채택**
- [시각자료] 실패(window 확대, 빨간 하락 화살표) vs 성공(곁눈질 확대, 초록 상승 화살표) 대조
- [발표 노트] "같은 '뷰를 늘린다'는 아이디어인데 방법에 따라 정반대 결과 — 이게 바로 다음 로드맵(dense-frame supervision)에 대한 경고이기도 함"

### 슬라이드 15 — Phase 8: "공짜" 최적화 발견
- 프로파일링 중 우연히 발견: **카메라 pose가 안 바뀌는데도 매 렌더링 호출마다 행렬 역산을 처음부터 다시 계산**하고 있었음
- pose가 바뀌는 지점이 코드 전체에 딱 한 곳뿐임을 확인한 뒤 캐싱 추가 — 그래디언트 수학은 전혀 안 건드리는 **무위험 변경**
- **결과: 시간 −3.0%, PSNR +0.52/+0.45dB, map() 처리 횟수 +38% — 전부 개선, 이 세션 최고 ROI**
- [시각자료] "무위험인데 3관왕" 강조 카드(시간/품질/coverage 아이콘 3개 체크)
- [발표 노트] "제일 큰 인프라 작업(다음 슬라이드)을 준비하다가 발견한 제일 작은데 제일 확실한 개선"

### 슬라이드 16 — Phase 8b: CUDA 커널 batch화 시도 — 정직한 실패 기록
- "카메라 여러 대를 한 번의 CUDA 커널 호출로 묶으면 더 빠르지 않을까" — 실제로 구현·검증까지 진행
- 기존 커널(그래디언트 수학 포함)은 안 건드리고 C++에서 안전하게 감싸는 방식으로 구현, **forward는 완전 일치, backward도 수치적으로 검증 통과**
- 실전 적용 1차 시도에서 PSNR 붕괴 발견 → 원인(텐서 shape 불일치) 진단·수정 → 재검증 통과
- **하지만 최종 시간 측정 결과 개선 없음** — 진짜 병목이 "CUDA 커널 실행 자체"라는 사전 진단이 재확인됨, **채택하지 않고 안전하게 원복**
- [시각자료] "구현→검증→실전 버그 발견→수정→재검증→최종 판정(속도 무변화)" 5단계 타임라인
- [발표 노트] "이걸 굳이 넣은 이유: 성공한 것만 보여주면 신뢰가 안 감. 위험한 시도를 안전하게 검증하고, 안 되면 안 되는 이유까지 확인하고 되돌리는 과정 자체가 이번 사이클의 방법론을 보여줌"

---

## §4. 종합 (슬라이드 17–19)

### 슬라이드 17 — 전체 타임라인
| 시점 | 실시간 배수 | PSNR(mean/kf) |
|---|---:|---:|
| 07/20 (exp52, 정정 후) | 1.52배 | — |
| exp53+54 | 0.94배(최초 실시간) | — |
| exp55 | ~0.92배 | 22.61 / 22.95 |
| **exp56 최종** | **0.70배** | **23.49 / 23.88** |
- [시각자료] 위 표를 라인차트로(x축 시간, y축 실시간 배수, 1.0배 기준선)
- [발표 노트] "실시간을 달성한 뒤에도 멈추지 않고 추가로 시간을 1/4 줄이면서 품질까지 올렸다"

### 슬라이드 18 — 현재 상태 스코어카드 + 다음 후보
- **현재**: 45.79초(예산 65.1초 대비 여유 19초), PSNR 23.49/23.88dB, carve loss로 floater 억제 유지 중
- **다음 후보(우선순위 논의 필요)**:
  1. CUDA 커널 레벨 batch화(Phase 8b가 실패한 진짜 원인 해결) — 고위험, 이득 불확실
  2. `init_itr_num`/`iters` 더 세밀한 스캔 — 저위험, 이득 작음
  3. Localization(흑백 SLAM 트래킹) 실제 통합 — 다음 큰 단계(exp50과 합류)
- [시각자료] 스코어카드 표 + 다음 후보 3개 우선순위 화살표
- [발표 노트] "1번은 리스크 대비 이득이 불확실해서 팀 논의가 필요한 지점"

### 슬라이드 19 — 결론 한 줄
- exp53+54: **실시간 최초 돌파**(0.94배)
- exp55: **품질 레버 확보**(gaussian −35.9%, floater −7.5%, 비용 없음)
- exp56: **"왜 안 빨라지나"를 끝까지 규명**해 4단계 연속 개선 + 실패한 시도(batch화)까지 안전하게 검증
- **한 줄 요약: 실시간을 달성하고, 그 위에서 시간을 추가로 1/4 더 줄이면서 품질까지 함께 올렸다**
- [시각자료] 없음
- [발표 노트] 다음 미팅까지 목표(localization 통합 vs 커널 batch화 여부)에 대한 팀 의견 요청으로 마무리
