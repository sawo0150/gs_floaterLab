# P05 — Cross-Scene 일반화 — 사전등록 계획 (v01, 2026-09-05)

> **사전등록.** 결과를 본 뒤 계획을 바꾸면 반드시 `newver.sh` 로 새 버전을 만들고,
> README 이력에 **왜 바꿨는지**를 남긴다. 결과를 보고 조용히 고치지 않는다.

## 이 실험이 답하는 질문

TODO — 한 문장.

## 세우려는 claim

A4 → [`../../../plan/claims/CURRENT.md`](../../../plan/claims/CURRENT.md)

## 비교 arm

최종 config 고정, 장면만 변경

**arm 간에 바뀌는 것은 위에 적은 것뿐이다.** 나머지(mapping loss, optimizer, PGBA,
Gaussian budget, terminal dust-GC, EOS rematuration 등)는 전부 동일하게 고정한다.

## 측정할 지표

held-out PSNR/SSIM/LPIPS, late temporal bins, 3회 mean±std

공통 지표와 strict 계약은 [`../../protocol/CURRENT.md`](../../protocol/CURRENT.md).

## 장면 / 반복

- 장면: aria1253, rot, 301_305, 12F
- seed 0,1,2 (3회), 표에는 mean ± std

## 성공/실패 판정 기준 (실험 **전에** 고정)

| 결과 | 판정 |
|---|---|
| TODO | TODO |

## 실패했을 때의 대응

TODO — `../../../plan/claims/CURRENT.md` 의 fallback과 연결.

## 실행

```bash
# TODO: scripts/run_remote.sh 로 디스패치
```

## 비고

⚠ exp59에서 aria1253 전용 튜닝이 rot −1.85dB, 305 16.95dB로 무너진 전례. **절대 프레임 경계 하드코딩 제거가 선결.**
